[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Optional: make sure client-side routing works on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
